services:
  cloudways-monitor:
    build: .
    env_file:
      - .env
    volumes:
      - cloudways-monitor-data:/data
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped

volumes:
  cloudways-monitor-data:
